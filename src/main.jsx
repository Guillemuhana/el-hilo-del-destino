import React from "react";
import ReactDOM from "react-dom/client";
import LenormandApp from "./LenormandApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LenormandApp />
  </React.StrictMode>
);
